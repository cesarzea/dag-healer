# Where the fake upstream API is listening.
SHOP_API_URL=http://localhost:8099

# Path to the Claude Code binary, if it is not simply `claude` on PATH.
# No API key is needed anywhere in this project: diagnosis runs through the
# Claude Code CLI on your own machine.
# CLAUDE_BIN=/usr/local/bin/claude
